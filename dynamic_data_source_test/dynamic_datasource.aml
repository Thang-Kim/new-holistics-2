const dynamic_data_source = if (H.current_user.data_source) {
                              H.current_user.data_source 
                            } else {
                              'demodb'
                            }